两个细节值得记：

懒加载：模块 import 时不建 LLM，第一次调用才建。原因是历史教训 —— 早期 import 就实例化 ChatOpenAI，CI 没有 .env 直接崩（git 提交 b1b5283 修复）。这也让 import src.agent 无副作用，测试友好。
__getattr__ 兼容层（90-96 行）：老代码可能有 from src.agent import cheap_llm 的访问方式，用模块级 __getattr__ 兜底，不破坏兼容。这是渐变改造的典型手法。