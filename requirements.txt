# ⚠️ 版本已锁定：本文件必须在本地验证全绿后才能修改（pytest 66 passed）
# 升级步骤：改版本号 → 本地 python -m pytest tests/ -q 全绿 → push（CI 自动复验）
# 历史事故：未锁版本导致 CI 装到 langgraph 1.x / langchain-core 1.x，
# 与 0.6.x 语义的代码不兼容，pytest 全挂。教训：LLM/图框架发版激进，必须锁。
# ⚠️ Python >= 3.10（MCP 官方 SDK 要求）；建议 3.12（本仓库 venv 标准）

fastapi==0.128.8
uvicorn==0.39.0
langgraph==0.6.11
langchain-openai==0.3.35
langchain-core==0.3.86
langsmith==0.4.37
# 企业级演进（2026-08）：ChromaDB → Qdrant（异步、并发、多租户隔离）
qdrant-client==1.19.0
# 业务库：SQLite → PostgreSQL（SQLAlchemy 2.0 async + asyncpg）
SQLAlchemy[asyncio]==2.0.52
asyncpg==0.30.0
# MCP 官方 SDK（异步客户端/服务端，要求 Python>=3.10 → 项目已升级 3.12）
mcp==1.28.1
sentence-transformers==5.1.2
torch==2.8.0
openai==2.44.0
python-dotenv==1.2.1
pydantic==2.13.4
pytest==8.4.2
pytest-asyncio==1.4.0
# 鉴权（2026-08）：JWT 签发/验签（pyjwt）+ 接口测试客户端（httpx，TestClient 依赖）
pyjwt==2.13.0
httpx==0.28.1