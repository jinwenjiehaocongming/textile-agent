# 18 · 高频追问与坑清单（面试速查）

> 目标：面试官大概率会在你讲完架构后追问这些"坑"。这里汇总全项目的诚实取舍与
> 可能被挑战的点，每条都给出"怎么答最加分"。配套：各步 Q&A + `面试/` 文件夹八股。

---

## 一、被追问时的万能框架（记住这个顺序）

1. **承认问题存在**（不装看不见）；
2. **一句话说清为什么当时这么选**（当时的约束：演示规模/演进路径/时间成本）；
3. **给出生产环境会怎么做**（可执行方案，哪怕没实现）；
4. 落回代码证据（文件:行号），证明你真读过。

面试官要的不是"零缺点的系统"，是**知道取舍且能自圆其说**的工程师。

## 二、坑清单（按模块归类）

### 鉴权与账号
| # | 坑 | 现实 | 生产怎么做 |
|---|---|---|---|
| 1 | JWT 无法主动吊销 | 改密码后旧 token 8h 内仍有效（auth.py exp=8h） | token 版本号/黑名单（Redis）、缩短有效期 |
| 2 | token 存 sessionStorage | XSS 能读走 | httpOnly Cookie + SameSite；或 CSP 严格化 |
| 3 | 登录无限流无锁定 | 可暴力破解（bcrypt 成本是唯一防线） | IP/账号限流、失败递增退避、验证码 |
| 4 | 登录响应时间差异 | 用户不存在时提前 return，比密码错快 ~0.3s，可被计时枚举 | 假哈希校验抹平时延 |
| 5 | 注册 6 位弱密码 | bcrypt 挡不住字典攻击 | 强度策略（zxcvbn）、2FA |
| 6 | `_secret()` DEV 默认密钥 | DEV_MODE 下固定密钥，仅限本地 | 生产强校验已做（非 DEV 缺失直接 RuntimeError） |

### 存储与数据
| # | 坑 | 现实 | 说明 |
|---|---|---|---|
| 7 | 时间存 TEXT ISO 字符串 | SQLite 迁移遗留；不能用 PG 时间函数 | 新表应 timestamptz |
| 8 | 应用层查重有 TOCTOU 窗口 | create_user 先查后插，并发会双插 | 靠 username UNIQUE 兜底（已做） |
| 9 | approval.py 待审批注册表在内存 | 重启丢失待审批单 | 生产要落库/Redis + 状态机表 |
| 10 | 审计失败静默 | 审计表写失败只打印 | 权衡：审计是增强非关键路径（有意为之） |

### Agent / 图
| # | 坑 | 现实 | 说明 |
|---|---|---|---|
| 11 | LLM 输出不稳定 | 意图分类/审核都可能错 | 规则层兜底（双层审核）、评测守护 |
| 12 | 流式 token 回调不能 await | put_nowait + maxsize=500 | 满队列抛错由外层兜底；背压取舍 |
| 13 | interrupt 恢复依赖 checkpointer | MemorySaver 内存版 | 生产用持久化 checkpointer（PG/Redis） |
| 14 | 内存态"挂起态守卫" | 只挡同一进程内的重复下单 | 多实例需分布式锁/状态查询 |

### 检索与 MCP
| # | 坑 | 现实 | 说明 |
|---|---|---|---|
| 15 | Rerank 默认关闭 | RERANK_ENABLED=0（本地模型 ~1GB 慢） | 生产接重排 API 或 GPU |
| 16 | MCP 子进程崩溃无重连 | 需确认 mcp_client 有无兜底 | 生产加重启/健康探活（如实核对代码） |
| 17 | 知识索引与源不同步 | chunks 变了要重跑 build_index | 脚本幂等，进 CI/entrypoint |
| 18 | Embedding 模型本地加载 | 首次下载 ~600MB、离线依赖 HF 缓存 | Docker 用 DOWNLOAD_MODELS + 挂缓存 |

### 运维 / 前端
| # | 坑 | 现实 | 说明 |
|---|---|---|---|
| 19 | CORS `allow_origins=["*"]` | 开发便利 | 生产收紧为具体域名 + 凭据策略 |
| 20 | 前端 60s 硬超时 | LLM 慢时可能误杀 | 可调；区分"超时"与"真失败" |
| 21 | SSE 连接数 | 长连接占资源 | 生产量级需网关层评估 |
| 22 | 静态托管顺序 | /api 路由必须先于 StaticFiles 注册 | 代码注释已强调（Starlette 顺序匹配） |

## 三、最容易翻车的 5 个"概念混淆"（说错立刻露馅）

1. **认证 vs 授权**：401 没证明你是谁；403 认识你但没资格。
2. **token 的三重含义**：登录 JWT（门禁卡）≠ LLM token（生成的字块）≠ 图中"状态更新"
   —— 嘴上分清，代码里是三个东西。
3. **SSE vs WebSocket**：SSE 服务器单向推、基于 HTTP、自动重连；WebSocket 双向。
   本项目的"打字机"用 SSE 足够（只需服务器→浏览器）。
4. **向量检索 vs 关键词检索**：语义相似 ≠ 字面命中；RRF 是融合不是排序模型。
5. **interrupt vs 普通 return**：interrupt 会**落盘挂起**（checkpoint），可被
   `Command(resume=...)` 唤醒继续跑；不是"发个消息等下次"。

## 四、讲项目时的"一页话术"（30 秒版 → 2 分钟版）

**30 秒**：我做的是一个纺织 B2B 交易智能体——客户用自然语言询价/下单/退货，
系统用 LangGraph 编排"改写→混合检索→意图路由→三 Agent 干活"的图，下单走到
人工审批（HITL interrupt），回复全部过双层审核并经 SSE 逐字推给前端；业务数据在
PostgreSQL，知识检索是 Qdrant 向量 + BM25 + RRF + Rerank，工具层是三个官方 MCP SDK
子进程，鉴权是 JWT 账号体系。

**2 分钟**（加演进与取舍）：这个项目是从演示客服机器人一步步演进来的——存储从
SQLite/ChromaDB 迁到 PostgreSQL/Qdrant（并发与独立服务），调用从同步线程桥迁到
全链路 asyncio（单事件循环），工具从函数直调迁到官方 MCP SDK（子进程标准化），
身份从 X-User-Id/开发登录迁到正式 JWT 账号体系（防冒充+审计），还加了多会话、
评估体系（检索消融 85 题、端到端 25 题、LLM-as-Judge 11 题全绿）和 Docker/CI。
每一步我都知道当时的动机和留下的取舍，比如 JWT 无法吊销、登录无限流、Rerank
默认关、审批注册表在内存——生产化清单里都列了对应方案。

## 五、如果被问"你最满意/最想改进哪一块"

- 最满意：**SSE 事件管道**——asyncio.Queue 统一节点事件与真 token 流，天然保序 +
  背压，一层解决"过程可视化 + 打字机"两个体验问题（详见 06 步）。
- 最想改进：**审批与状态持久化**——待审批在内存、checkpointer 用 MemorySaver，
  重启即丢；生产应落 PG/Redis 并加分布式守卫（详见 08 步坑）。
